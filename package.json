{
  "name": "uke-app",
  "version": "0.0.1",
  "description": "UKE sports management application",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/shared"
  ]
}
